{terminal.color('light-green')}  [ OK ]  {terminal.color('reset')}{terminal.color('white')}Cron stopped. {terminal.color('reset')}

use {terminal.color('white')}{binary()} service cron start {terminal.color('reset')}to start the cron jobs
