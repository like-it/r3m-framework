{
  "package" : {
    "r3m-io/priya" : {
        "composer" : "composer require {{$this.parent.key}}",
        "route" : [
          "{{config('project.dir.vendor') + $this.parent.key + 'Data/Route.json'}}"
        ],
        "installation" : "{{config('project.dir.data') + config('dictionary.package') + config('ds') + $this.parent.key + config('ds') + config('dictionary.installation') + config('extension.json')}}",
        "command" : [
            "{{binary()}} {{$this.parent.key}} setup"
        ]
    }
  }
}