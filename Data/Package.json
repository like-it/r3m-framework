{
  "package" : {
    "r3m-io/priya" : {
        "composer" : "composer require {{$this.parentNode.key}}",
        "route" : [
          "{{config('project.dir.vendor') + $this.parentNode.key + 'Data/Route.json'}}"
        ],
        "installation" : "{{config('project.dir.data') + config('dictionary.package') + config('ds') + $this.parentNode.key + config('ds') + config('dictionary.installation') + config('extension.json')}}",
        "command" : [
            "{{binary()}} {{$this.parentNode.key}} setup"
        ]
    }
  }
}